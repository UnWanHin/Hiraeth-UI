# Agent Instructions

This project uses `AGENTS.md` as the canonical agent instruction file.

Read `AGENTS.md` before working in this repository. In particular, keep `docs/` and `docs/issues/` updated during debugging, implementation, verification, and rollback decisions.
